import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  typography: {
    allVariants: {
      color: '#4a4a4a',
    },
  },
  palette: {
    background: {
      default: '#fff',
    },
    primary: {
      main: '#0F1108',
    },
    secondary: {
      main: '#E8DB7D',
    },
  },
});
